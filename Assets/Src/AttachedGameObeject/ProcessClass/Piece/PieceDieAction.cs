using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PieceDieAction : MonoBehaviour
{
    /*<責務>
     *オブジェクトの破壊時の処理を行う
     */
    [SerializeField] ObjectReference objectReference;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //合成時の処理を行うー＞オブジェクトと衝突したオブジェクトは見た目的に消える
    public void PieceSyntghesisConditionsLaterDie()
    {

    }
}
